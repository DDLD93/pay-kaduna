# ============================================================================
# PayKaduna Integration Microservice - Environment Variables
# ============================================================================
# 
# Copy this file to .env and update with your actual values
# cp sample.env .env
# 
# ============================================================================

# Application Environment
# Options: development, production, test
NODE_ENV=development

# Server Port
# Default: 3000
PORT=3000

# Application Domain URL (Optional)
# Used for Swagger documentation server URLs
# If not provided, defaults to http://localhost:{PORT}
# Example: https://api.example.com
DOMAIN_URL=https://api.example.com

# ============================================================================
# PayKaduna Configuration
# ============================================================================

# PayKaduna Test API Base URL
# Used when NODE_ENV is 'development' or 'test'
PK_BASE_URL_TEST=https://testingapi.paykaduna.com/

# PayKaduna Production API Base URL
# Used when NODE_ENV is 'production'
PK_BASE_URL_PROD=https://api.paykaduna.com/

# PayKaduna API Key (Secret Key)
# Required for HMAC signature generation
# This is the secret used to sign all API requests
# Obtain from PayKaduna support
PK_API_KEY=your_api_key_here

# PayKaduna Engine Code
# Required for bill creation
# Injected into bill requests if not provided in the request body
# Obtain from PayKaduna support
PK_ENGINE_CODE=your_engine_code_here

# PayKaduna Webhook Secret Key (Optional)
# Used for validating incoming webhook signatures
# If not provided, PK_API_KEY will be used as fallback
# Obtain from PayKaduna support
PK_WEBHOOK_SECRET_KEY=your_webhook_secret_key_here

# ============================================================================
# Notes:
# ============================================================================
# 
# 1. All PayKaduna variables (PK_*) are REQUIRED
# 2. The service will automatically use:
#    - PK_BASE_URL_TEST when NODE_ENV is 'development' or 'test'
#    - PK_BASE_URL_PROD when NODE_ENV is 'production'
# 3. PK_API_KEY is used for HMAC-SHA256 signature generation on all requests
# 4. PK_ENGINE_CODE will be automatically injected if not provided in bill requests
# 5. DOMAIN_URL is optional - if provided, Swagger documentation will use it for server URLs
# 6. Never commit the actual .env file to version control
# 
# ============================================================================
